{
  "servers": {
    "github": {
      "enabled": true,
      "type": "local",
      "description": "GitHub repository and file management via PAT",
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}",
        "GITHUB_ORG": "${GITHUB_ORG:-academic-idp}"
      }
    },
    "kubernetes": {
      "enabled": true,
      "type": "local",
      "description": "Kubernetes cluster interaction via kubeconfig (Docker Desktop)",
      "env": {
        "KUBECONFIG": "${KUBECONFIG:-}"
      }
    },
    "argocd": {
      "enabled": true,
      "type": "local",
      "description": "ArgoCD GitOps monitoring (stub/mock mode for Phase 1.5)",
      "env": {
        "ARGOCD_URL": "${ARGOCD_URL:-}",
        "ARGOCD_TOKEN": "${ARGOCD_TOKEN:-}"
      }
    },
    "policy": {
      "enabled": true,
      "type": "local",
      "description": "Institution-specific RBAC policy engine and cost estimator"
    },
    "terraform": {
      "enabled": true,
      "type": "local",
      "description": "Terraform IaC configuration management, formatting, validation, and planning",
      "env": {
        "IAC_DIR": "${IAC_DIR:-/app/iac}"
      }
    },
    "cicd": {
      "enabled": true,
      "type": "local",
      "description": "GitHub Actions CI/CD workflow management and validation",
      "env": {
        "WORKFLOWS_DIR": "${WORKFLOWS_DIR:-/app/.github/workflows}"
      }
    }
  }
}

apiVersion: v1
kind: Service
metadata:
  name: idp-backend-svc
  labels:
    app: idp-backend
    app.kubernetes.io/part-of: academic-idp
spec:
  type: ClusterIP
  selector:
    app: idp-backend
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
